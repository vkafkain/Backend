const Response = require('../models/Response')
const GroundOperation = require('../models/GrounOperation')

const postGO = async (req, res) => {
    const data = req.body.data
    return null;
}

module.exports = { postGO }